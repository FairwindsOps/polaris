apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: plaintext-to-tls-backend
  namespace: app
spec:
  hostnames:
  - app.example.com
  rules:
  - backendRefs:
    - name: api
      port: 443
