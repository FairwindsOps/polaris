apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: fail-closed
spec:
  gatewayClassName: kgateway
  tls:
    frontend:
      default:
        validation:
          mode: AllowValidOnly
  listeners:
  - name: https
    protocol: HTTPS
    port: 443
