apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: fail-open
spec:
  gatewayClassName: kgateway
  tls:
    frontend:
      default:
        validation:
          mode: AllowInsecureFallback
  listeners:
  - name: https
    protocol: HTTPS
    port: 443
