package {{ .name.Package }}

// Options for generating {{.name}}
type Options struct {
  // add your stuff here
}


// Validate that options are usuable
func (opts *Options) Validate() error {
  return nil
}

